from ...util.structures.Equippable import Equippable
from ...util.structures.Stats import Stats


helms = {
    'copper helm': Equippable(
        name='copper helm',
        tier=1,
        stats=Stats({
            'physical_defense': 1,
        })
    )
}
